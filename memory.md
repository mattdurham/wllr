I want to implement memory, this should be modernc sqlite with vector support. It should support hiearchical memory. So L1 is the current prompt, open files etc. L2 is the ideal "Go coder utilizing...", l3 could be "when go coding always ensure you close channels properly", l4 is specific as in live_store.go has an issue with X normally". L5 is raw transcript. I want the ability to compact data to higher level. Actually L1 should be high level personas, ie what is fundamental. Should use TOON notation, exportable, and scored as float from +1 to -1. Both +1 and -1 should be unattainabled but a perfect positive experience that required no refinement would be 0.999, whereas a entirely negative approach would be -0.999. 0 would be nuetral, we generally care about those at either end of the spectrum. This should be vector/rag. History should be automatic from the transcript with tools to explicitly save a memory. 
