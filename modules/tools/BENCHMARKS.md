# tools — Benchmark Specifications

## Metric Targets

| Benchmark | Target | Notes |
|-----------|--------|-------|
| `BenchmarkBuildFantasyTools_100Tools` | < 1ms | Schema parsing for 100 tools |
| `BenchmarkParseInputSchema` | < 10μs per call | JSON unmarshaling cost |

## Existing Benchmarks

None yet.
